spring:
  application:
    name: bibliotheque
  datasource:
    url: jdbc:postgresql://localhost:5432/bibliotheque
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  mail:
    host: localhost
    port: 1025
    username:
    password:

server:
  port: 8080
